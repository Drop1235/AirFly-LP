# AirFry シークレットセール用ゲート機能 ＆ ビジュアルオーダーフロー 設計ドキュメント

## 1. 目的と概要
本ドキュメントは、「AirFry販売サイト」におけるLINEログインゲート機能、およびGoogleフォームを廃止しサイト内で完結する「3ステップ・ビジュアル・オーダーフロー（＋スプレッドシート連携）」の実装設計仕様を定義するものです。
この設計に基づき、`index.html`内でLIFF APIを利用した状態遷移と、画像や特徴表示を伴う直感的なオーダーフロー、およびGoogle Apps Script (GAS)を介したスプレッドシートへの直接送信を実現します。

## 2. デザイン・配色の要件
*   **カラースキーム**: 「白・ブルー基調」（健康的・信頼性・アクティブ）。ブラック系は避け、明るく洗練されたクリーンなイメージを持たせる。
*   **ビジュアル・オーダーUI**: 各選択肢（モデル、レンズ、フレーム）は、上部に写真、下部に名前と特徴テキストが入る洗練されたカードデザイン（白背景・シャドウ付き）を採用。
*   **選択の可視化**: カードクリック時に枠線が太くなり、チェックマークが表示されるなど、現在の選択状態が一目でわかるようにする。
*   **アニメーション**: ステップ間の遷移はフェードインなどの滑らかな動き（トランジション）を実装する。

## 3. UI/UXフロー設計（LINEログイン ＋ ビジュアルオーダーフロー）

### 3-1. ゲート機能（LIFF連携）
アクセス直後はゲートUI（白とブルーを基調としたライトテーマ、全画面中央配置）のみを表示し、メインコンテンツは非表示にします。
以下の手順で認証を完了させます。
1.  **ステップ1（LINEログイン）**: 未ログインユーザーに表示。「LINEでログイン」ボタンでLIFF認証へ。
2.  **ステップ2（メールアドレス登録）**: ログイン済・未登録ユーザーに表示。メールアドレスを入力して同意。

### 3-2. ビジュアル・オーダーフロー（メインコンテンツ内）
ゲート通過後に、Heroセクション（ファーストビュー）を含むメインコンテンツ全体が表示されます。注文セクションは以下の4ステップで構成されます。

*   **ステップ1：モデル選択**
    *   BK / WV などの各モデルをカード型で一覧表示。
    *   カード構成: 大きなモデル写真、モデル名、3つの特徴（箇条書き）。
    *   選択後、「次へ（レンズ選択）」ボタンがアクティブになる。

*   **ステップ2：レンズ選択**
    *   ステップ1で選択したモデルに応じたレンズ候補のみを動的に表示。
    *   カード構成: レンズ写真、レンズ名、機能的特徴。
    *   「戻る」ボタンと「次へ（フレーム選択）」ボタンを配置。

*   **ステップ3：フレーム選択**
    *   フレームのバリエーションをカード型で表示。
    *   カード構成: フレーム写真、フレーム名。
    *   「戻る」ボタンと「次へ（最終確認）」ボタンを配置。

*   **ステップ4：最終確認・スプレッドシート送信**
    *   選択したモデル、レンズ、フレーム、計算された特別価格、および取得済みのメールアドレスを一覧表示。
    *   「注文を確定する」ボタン押下で、Google Apps Script (GAS) を介してスプレッドシートへデータをPOST送信。
    *   送信完了後、サンクスメッセージを表示。

## 4. データフロー設計（GAS スプレッドシート連携）
Googleフォームによる注文を廃止し、自前のAPI（GAS Webアプリ）へ直接送信します。

1.  **データ収集**:
    *   メールアドレス（`localStorage` から取得）
    *   選択モデル
    *   選択レンズ
    *   選択フレーム
    *   計算された価格
2.  **POST送信**:
    *   JavaScriptの `fetch` API を使用し、JSON形式でGASのWebアプリURLへPOSTリクエストを送信。
3.  **GAS側の処理 (`Code.gs`)**:
    *   `doPost(e)` 関数でリクエストを受け取り、JSONをパース。
    *   指定されたスプレッドシート（ID: `1Wu0RyGpWtIUBsH0tl9dQwt7DX4e5EsZHazJ8b5yTk6Q`）のシートに `appendRow` で行を追加。
    *   CORS対応のためのヘッダー付きレスポンスを返す。

## 5. コンポーネント構造・DOM定義 (Order Flow部分)
```html
<section id="visual-order-flow">
  <!-- ステップ1: モデル選択 -->
  <div id="flow-step-1" class="flow-step active">
    <h3>1. モデルを選択</h3>
    <div class="card-grid" id="model-cards">
      <!-- カードUI: img, h4, ul(特徴) -->
    </div>
    <button onclick="nextStep(2)">次へ</button>
  </div>

  <!-- ステップ2: レンズ選択 -->
  <div id="flow-step-2" class="flow-step" style="display:none;">
    <h3>2. レンズを選択</h3>
    <div class="card-grid" id="lens-cards"></div>
    <button onclick="prevStep(1)">戻る</button>
    <button onclick="nextStep(3)">次へ</button>
  </div>

  <!-- ステップ3: フレーム選択 -->
  <div id="flow-step-3" class="flow-step" style="display:none;">
    ...
  </div>

  <!-- ステップ4: 最終確認 -->
  <div id="flow-step-4" class="flow-step" style="display:none;">
    <h3>ご注文内容の確認</h3>
    <div id="order-summary"></div>
    <button onclick="sendOrderToSheet()">注文を確定する</button>
  </div>
</section>
```
この設計により、ユーザーは視覚的で迷いのない操作が可能となり、コンバージョン率の向上が期待できます。
