# AirFry シークレットセール用ゲート機能 ＆ ビジュアルオーダーフロー 設計ドキュメント

## 1. 目的と概要
本ドキュメントは、「AirFry販売サイト」におけるLINEログインゲート機能、およびGoogleフォームを廃止しサイト内で完結する「3ステップ・ビジュアル・オーダーフロー（＋スプレッドシート連携）」の実装設計仕様を定義するものです。
この設計に基づき、`index.html`内でLIFF APIを利用した状態遷移と、画像や特徴表示を伴う直感的なオーダーフロー、およびGoogle Apps Script (GAS)を介したスプレッドシートへの直接送信を実現します。

## 2. デザイン・配色の要件
*   **カラースキーム**: 「白・ブルー基調」（健康的・信頼性・アクティブ）。ブラック系は避け、明るく洗練されたクリーンなイメージを持たせる。
*   **ビジュアル・オーダーUI**: 各選択肢（モデル、レンズ、フレーム）は、上部に写真、下部に名前と特徴テキストが入る洗練されたカードデザイン（白背景・シャドウ付き）を採用。
*   **選択の可視化**: 各ステップにおける選択は**単一選択（排他選択）**とし、カードクリック時に枠線が太くなり、チェックマークが表示されるなど、現在の選択状態が一目でわかるようにする。
*   **アニメーション**: ステップ間の遷移はフェードインなどの滑らかな動き（トランジション）を実装する。

## 3. UI/UXフロー設計（LINEログイン ＋ ビジュアルオーダーフロー）

### 3-1. ゲート機能（LIFF連携）
アクセス直後はゲートUI（白とブルーを基調としたライトテーマ、全画面中央配置）のみを表示し、メインコンテンツは非表示にします。
以下の手順で認証を完了させます。
1.  **ステップ1（LINEログイン）**: 未ログインユーザーに表示。「LINEでログイン」ボタンでLIFF認証へ。
2.  **ステップ2（メールアドレス登録）**: ログイン済・未登録ユーザーに表示。メールアドレスを入力して同意。

### 3-2. ビジュアル・オーダーフロー（メインコンテンツ内）
ゲート通過後に、Heroセクション（ファーストビュー）を含むメインコンテンツ全体が表示されます。注文セクションは以下の4ステップで構成されます。

*   **ステップ1：モデル選択**
    *   ターゲット層別にカテゴリ分けして表示（男性用、女性・ジュニア用、キッズ用）。
    *   BK / WV / AF-301 などの各モデルをカード型で一覧表示。
    *   カード構成: 大きなモデル写真、モデル名、サブタイトル、特徴（箇条書き）、レンズ幅。
    *   選択後、選択したモデル名がバッジとして表示され、次のステップへ遷移可能になる。

*   **ステップ2：レンズ選択**
    *   ステップ1で選択したモデルに応じたレンズ候補を動的に表示。
    *   **選択仕様**: 通常レンズと偏光レンズのカテゴリを跨いで1つのみ選択可能。
    *   表示順：可視光線透過率が低い順（暗いレンズ順）にソートして表示。
    *   カテゴリ分け：「通常レンズ」と「偏光レンズ」に分けて表示。
    *   共通仕様：全レンズ紫外線を99％以上カットすることをステップ冒頭に明記。
    *   偏光レンズ：カテゴリ全体の共通説明と、通常レンズとの価格差（+4,400円）を明記。

*   **ステップ3：フレーム選択**
    *   ステップ1・2での選択内容を上部にバッジ表示。
    *   フレームの全バリエーションをカード型で表示。
    *   カード構成: フレームの実際の製品写真、フレーム名。
    *   「カートに追加して確認へ」ボタンで商品をカート（配列）に保存し、ステップ4へ遷移。

*   **ステップ4：カート確認と決済画面への遷移**
    *   カートに追加された複数の商品（モデル、レンズ、フレーム）をリスト形式で表示。
    *   「別の商品を追加する」ボタンで、現在の商品を保持したままステップ1へ戻り、追加購入を可能にする。
    *   各カート商品に「削除ボタン（ゴミ箱アイコン）」を設置し、個別削除に対応。
    *   価格計算ロジック（フロントエンドでの表示用・バックエンドでの決済用共通）:
        *   **AF-302-WP**: 一律 23,760円
        *   **AF-901**: 一律 14,850円
        *   **その他のモデル**: 通常レンズ 18,000円 / 偏光レンズ 22,000円
    *   合計金額（定価および特別価格）を算出して表示。
    *   「決済に進む」ボタン押下で、セキュリティ上の理由から価格情報は送信せず、商品情報（モデル、レンズ、フレーム）のみをNetlify Functionsへ送信。

## 4. データフロー設計（Stripe決済 ＋ GAS連携）
Googleフォームによる注文を廃止し、自前のAPI（Netlify Functions経由）でのStripe決済とスプレッドシート連携を実現します。

1.  **データ収集**:
    *   メールアドレス（`localStorage` から取得）
    *   選択された商品の配列（`model`, `lens`, `frame` のみ）
    *   ※価格改ざんを防ぐため、フロントエンドで計算した価格情報は送信しない。
2.  **決済セッション作成 (Netlify Functions: `create-checkout-session.js`)**:
    *   JavaScriptの `fetch` API を使用し、JSON形式でNetlify FunctionsへPOSTリクエストを送信。
    *   バックエンド側で商品のカタログデータ（`getPriceData`）を用いて正しい価格を再計算。
    *   再計算された安全な価格を用いて Stripe Checkout Session を作成し、決済画面URLをフロントエンドに返却・リダイレクト。
    *   購入商品データはStripeセッションの `metadata` に保存。
3.  **GASへの書き込み (Netlify Functions: `stripe-webhook.js`)**:
    *   Stripe上で決済が正常に完了した際、`checkout.session.completed` イベントがWebhookとして送信される。
    *   Webhookを受け取ったサーバーが、メタデータに保存されていた購入商品データとメールアドレスを取り出し、GAS WebアプリURLへPOSTリクエストを送信。
4.  **GAS側の処理 (`Code.gs`)**:
    *   送られてきた商品配列（items）をループ処理し、1商品につき1行ずつスプレッドシートに追記する。

## 5. コンポーネント構造・DOM定義 (Order Flow部分)
```html
<section id="visual-order-flow">
  <!-- ステップ1: モデル選択 -->
  <div id="flow-step-1" class="flow-step active">
    <h3>1. モデルを選択</h3>
    <div class="card-grid" id="model-cards">
      <!-- カードUI: img, h4, ul(特徴) -->
    </div>
    <button onclick="nextStep(2)">次へ</button>
  </div>

  <!-- ステップ2: レンズ選択 -->
  <div id="flow-step-2" class="flow-step" style="display:none;">
    <h3>2. レンズを選択</h3>
    <div id="lens-selection-container">
      <!-- カテゴリごとの見出しとcard-gridが動的に生成される -->
    </div>
    <button onclick="prevStep(1)">戻る</button>
    <button onclick="nextStep(3)">次へ</button>
  </div>

  <!-- ステップ3: フレーム選択 -->
  <div id="flow-step-3" class="flow-step" style="display:none;">
    ...
  </div>

  <!-- ステップ4: 最終確認 -->
  <div id="flow-step-4" class="flow-step" style="display:none;">
    <h3>ご注文内容の確認</h3>
    <div id="order-summary"></div> <!-- 複数商品がリスト表示される -->
    <button onclick="addAnotherItem()">別の商品を追加する</button>
    <button onclick="submitOrder()">決済に進む</button>
  </div>
</section>
```
この設計により、ユーザーは直感的な操作で複数商品をスムーズにカートへ追加でき、セキュアなバックエンドでの価格計算により改ざんのない安全なクレジットカード決済と注文管理が実現されます。

